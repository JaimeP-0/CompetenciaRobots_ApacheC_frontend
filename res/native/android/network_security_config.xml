<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- API en Tailscale/LAN sin TLS (HTTP). Si cambia la IP del servidor, no hace falta tocar esto. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
